<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.knead_yourself">

    <application
        android:allowBackup="true"
        android:icon="@drawable/maingym"
        android:label="@string/app_name"
        android:roundIcon="@drawable/maingym"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <receiver android:name=".NotificationPublisher" android:process=":remote"/>
        <activity android:name=".NotificaionActivity"/>
        <activity android:name=".AddActivity" />
        <activity android:name=".ExerciseActivity" />
        <activity android:name=".TrainingActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>